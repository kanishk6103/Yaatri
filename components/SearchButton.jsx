import { View, Text } from 'react-native'
import React from 'react'
import { Button } from "react-native";
const SearchButton = () => {
  return (
       <Button className="flex bg-Success py-3 px-3 justify-center items-center self-stretch rounded-xl" title="Search Me" onPress={() => {}} />
  )
}

export default SearchButton